#include <stdio.h>

int main()
{
    int a = 3, b = 7, c;

//  a = a+1;    // a değişkenini 1 arttırır
//  ++a;        // a değişkenini 1 arttırır
    a += 1;     // a deşğişkenini 1 arttırır
    printf("a %d\n, a");

    b *= a;     // b = b*a; 

    printf("b %d\n, b" );



    getchar();
    return 0;
}